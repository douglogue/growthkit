<nav class="navbar <%= navbar_bg %>">
  <button class="navbar-toggler hidden-sm-up" type="button" data-toggle="collapse" data-target="#exCollapsingNavbar2">
    &#9776;
  </button>
  <div class="collapse navbar-toggleable-xs" id="exCollapsingNavbar2">
    <a class="navbar-brand" href="/">GrowthKit</a>
    <% if user_signed_in? %>
      <ul class="nav navbar-nav">
        <li class="nav-item"><%= link_to 'Landing Pages', landings_path, class: 'nav-link' %></li>
        <li class="nav-item"><%= link_to 'Promo Bars', promo_bars_path, class: 'nav-link' %></li>
        <li class="nav-item"><%= link_to 'Leads', leads_path, class: 'nav-link' %></li>
      </ul>
    <% end %>
    <ul class="nav navbar-nav pull-xs-right">
      <% if user_signed_in? %>
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" id="accountDropdown" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
            <%= current_user.email %>
          </a>
          <div class="dropdown-menu" aria-labelledby="accountDropdown">
            <%= link_to 'Admin', admin_root_path, class: 'dropdown-item' unless current_user.admin != true %>
            <%= link_to 'Account', edit_user_registration_path, class: 'dropdown-item' %>
            <%= link_to 'Logout', destroy_user_session_path, class: 'dropdown-item', :method => :delete %>
          </div>
        </li>
      <% else %>
        <li class="nav-item"><%= link_to 'Sign in', new_user_session_path, class: 'btn btn-link' %></li>
        <li class="nav-item"><%= link_to 'Sign up', new_user_registration_path, class: 'btn btn-primary' %></li>
      <% end %>
    </ul>
  </div>
</nav>
